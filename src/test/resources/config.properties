## domain configuration
domain.base.url=${domain.base.url}
##

## browser configuration
browser.wait=${browser.wait}
#chrome, safari,
browser.type=${browser.type}
browser.version=${browser.version}
browser.size=${browser.size}
browser.maximize=${browser.maximize}
browser.remote=${browser.remote}
browser.remote.url=${browser.remote.url}
capability.name=${capability.name}
##

## api configuration
api.base.url=${api.base.url}
api.base.path=${api.base.path}

#authorization variables
api.corp.id=${api.corp.id}
api.corp.id.value=${api.corp.id.value}
api.token.name=${api.token.name}
api.token.value=${api.token.value}

api.logs.print=false
##

## hibernate configuration
hibernate.connection.url=${hibernate.connection.url}
hibernate.connection.username=${hibernate.connection.username}
hibernate.connection.password=${hibernate.connection.password}
hibernate.connection.driver.class=${hibernate.connection.driver.class}
hibernate.dialect=${hibernate.dialect}
javax.persistence.lock.timeout=${javax.persistence.lock.timeout}
javax.persistence.query.timeout=${javax.persistence.query.timeout}
hibernate.show.sql=${hibernate.show.sql}
hibernate.format.sql=${hibernate.format.sql}
##